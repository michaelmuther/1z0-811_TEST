public class JavaBasics4 {
}
/*
A Java method ....
Answered Incorrectly

You had to select 2 option(s)
cannot return multiple values. // TRUE

cannot be private. // FALSE
You may apply any access modifer (public, private, protected) to a method.

must take 1 or more parameters. // FALSE
It may take zero parameters also.

must return a value. // FALSE
It may return void i.e. no value at all.

must exist inside a type definition. // TRUE
Only a class, interface, or enum are allowed to have methods. You cannot just have a method without any enclosing type.
 */